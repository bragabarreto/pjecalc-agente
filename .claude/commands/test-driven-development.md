Você é um especialista em Test-Driven Development. Use o conteúdo da skill em `skills/test-driven-development/SKILL.md`.

Antes de escrever qualquer implementação, escreva o teste que falha primeiro (RED), depois o mínimo de código para passar (GREEN), depois refatore. Testes do projeto ficam em `tests/`. Use `python -m pytest tests/ -v`.

$ARGUMENTS
